# ③ ロバストネス図

作成ルール：[docs/03_ロバストネス図の書き方.md](../docs/03_ロバストネス図の書き方.md)

凡例：`(( ))`＝アクター、`[[ ]]`＝バウンダリ（画面）、`{{ }}`＝コントローラ、`[( )]`＝エンティティ。実線＝基本フロー、破線＝代替フロー。エンティティ名は日本語（実装名は書かない。実装対応は[01_ドメインモデル.md](01_ドメインモデル.md)を参照）。

## 1. プロフィールを閲覧する

```mermaid
flowchart LR
    actor((訪問者))
    b1[[プロフィール画面]]
    c1{{プロフィールを閲覧する}}
    e1[(活動実績)]
    e2[(資格)]
    e3[(スキル)]

    actor -->|アクセスする| b1
    b1 -->|情報取得を要求| c1
    c1 -->|一覧を取得する| e1
    e1 -->|一覧を返却| c1
    c1 -->|一覧を取得する| e2
    e2 -->|一覧を返却| c1
    c1 -->|一覧を取得する| e3
    e3 -->|一覧を返却| c1
    c1 -->|情報を表示する| b1
    c1 -.->|"［未登録の場合］空欄を表示する"| b1
```

## 2. 管理者としてログインする

```mermaid
flowchart LR
    actor((管理者))
    b1[[ログイン画面]]
    b2[[管理画面]]
    c1{{管理者としてログインする}}
    e1[(管理者)]

    actor -->|ログイン情報を入力する| b1
    b1 -->|認証を要求| c1
    c1 -->|認証情報を照合する| e1
    e1 -->|照合結果を返却| c1
    c1 -->|遷移させる| b2
    c1 -.->|"［認証失敗の場合］エラーを表示する"| b1
```

## 3. 管理者からログアウトする

```mermaid
flowchart LR
    actor((管理者))
    b1[[管理画面]]
    b2[[プロフィール画面]]
    c1{{管理者からログアウトする}}
    e1[(管理者)]

    actor -->|ログアウトを選択する| b1
    b1 -->|ログアウトを要求| c1
    c1 -->|認証状態を解除する| e1
    e1 -->|完了| c1
    c1 -->|遷移させる| b2
```

## 4. 活動実績を登録する

```mermaid
flowchart LR
    actor((管理者))
    b1[[活動実績登録画面]]
    b2[[管理画面]]
    c1{{活動実績を登録する}}
    e1[(活動実績)]
    e2[(タグ)]

    actor -->|開く| b1
    b1 -->|登録を要求| c1
    c1 -->|タグを取得または作成する| e2
    e2 -->|タグを返却| c1
    c1 -->|内容を検証し登録する| e1
    e1 -->|完了| c1
    c1 -->|登録結果を表示する| b2
    c1 -.->|"［入力不備の場合］エラーを表示する"| b1
```

## 5. 活動実績を編集する

```mermaid
flowchart LR
    actor((管理者))
    b1[[管理画面]]
    b2[[活動実績編集画面]]
    c1{{活動実績を編集する}}
    e1[(活動実績)]
    e2[(タグ)]

    actor -->|編集対象を選択する| b1
    b1 -->|編集画面表示を要求| c1
    c1 -->|対象の内容を取得する| e1
    e1 -->|内容を返却| c1
    c1 -->|現在の内容を表示する| b2
    actor -->|内容を変更する| b2
    b2 -->|更新を要求| c1
    c1 -->|タグを取得または作成する| e2
    e2 -->|タグを返却| c1
    c1 -->|内容を検証し更新する| e1
    e1 -->|完了| c1
    c1 -->|更新結果を表示する| b1
    c1 -.->|"［入力不備の場合］エラーを表示する"| b2
```

## 6. 活動実績を削除する

```mermaid
flowchart LR
    actor((管理者))
    b1[[管理画面]]
    b2[[活動実績削除確認画面]]
    c1{{活動実績を削除する}}
    e1[(活動実績)]

    actor -->|削除対象を選択する| b1
    b1 -->|削除確認画面表示を要求| c1
    c1 -->|対象の内容を取得する| e1
    e1 -->|内容を返却| c1
    c1 -->|対象の内容を表示する| b2
    actor -->|削除を確定する| b2
    b2 -->|削除を要求| c1
    c1 -->|削除する| e1
    e1 -->|完了| c1
    c1 -->|削除結果を表示する| b1
    c1 -.->|"［キャンセルの場合］管理画面へ戻す"| b1
```

## 7. 資格情報を登録する

```mermaid
flowchart LR
    actor((管理者))
    b1[[資格登録画面]]
    b2[[管理画面]]
    c1{{資格情報を登録する}}
    e1[(資格)]

    actor -->|開く| b1
    b1 -->|登録を要求| c1
    c1 -->|内容を検証し登録する| e1
    e1 -->|完了| c1
    c1 -->|登録結果を表示する| b2
    c1 -.->|"［入力不備の場合］エラーを表示する"| b1
```

## 8. 資格情報を編集する

```mermaid
flowchart LR
    actor((管理者))
    b1[[管理画面]]
    b2[[資格編集画面]]
    c1{{資格情報を編集する}}
    e1[(資格)]

    actor -->|編集対象を選択する| b1
    b1 -->|編集画面表示を要求| c1
    c1 -->|対象の内容を取得する| e1
    e1 -->|内容を返却| c1
    c1 -->|現在の内容を表示する| b2
    actor -->|内容を変更する| b2
    b2 -->|更新を要求| c1
    c1 -->|内容を検証し更新する| e1
    e1 -->|完了| c1
    c1 -->|更新結果を表示する| b1
    c1 -.->|"［入力不備の場合］エラーを表示する"| b2
```

## 9. 資格情報を削除する

```mermaid
flowchart LR
    actor((管理者))
    b1[[管理画面]]
    b2[[資格削除確認画面]]
    c1{{資格情報を削除する}}
    e1[(資格)]

    actor -->|削除対象を選択する| b1
    b1 -->|削除確認画面表示を要求| c1
    c1 -->|対象の内容を取得する| e1
    e1 -->|内容を返却| c1
    c1 -->|対象の内容を表示する| b2
    actor -->|削除を確定する| b2
    b2 -->|削除を要求| c1
    c1 -->|削除する| e1
    e1 -->|完了| c1
    c1 -->|削除結果を表示する| b1
    c1 -.->|"［キャンセルの場合］管理画面へ戻す"| b1
```

## 10. スキルを登録する

```mermaid
flowchart LR
    actor((管理者))
    b1[[スキル登録画面]]
    b2[[管理画面]]
    c1{{スキルを登録する}}
    e1[(スキル)]

    actor -->|開く| b1
    b1 -->|登録を要求| c1
    c1 -->|内容を検証し登録する| e1
    e1 -->|完了| c1
    c1 -->|登録結果を表示する| b2
    c1 -.->|"［入力不備の場合］エラーを表示する"| b1
```

## 11. スキルを編集する

```mermaid
flowchart LR
    actor((管理者))
    b1[[管理画面]]
    b2[[スキル編集画面]]
    c1{{スキルを編集する}}
    e1[(スキル)]

    actor -->|編集対象を選択する| b1
    b1 -->|編集画面表示を要求| c1
    c1 -->|対象の内容を取得する| e1
    e1 -->|内容を返却| c1
    c1 -->|現在の内容を表示する| b2
    actor -->|内容を変更する| b2
    b2 -->|更新を要求| c1
    c1 -->|内容を検証し更新する| e1
    e1 -->|完了| c1
    c1 -->|更新結果を表示する| b1
    c1 -.->|"［入力不備の場合］エラーを表示する"| b2
```

## 12. スキルを削除する

```mermaid
flowchart LR
    actor((管理者))
    b1[[管理画面]]
    b2[[スキル削除確認画面]]
    c1{{スキルを削除する}}
    e1[(スキル)]

    actor -->|削除対象を選択する| b1
    b1 -->|削除確認画面表示を要求| c1
    c1 -->|対象の内容を取得する| e1
    e1 -->|内容を返却| c1
    c1 -->|対象の内容を表示する| b2
    actor -->|削除を確定する| b2
    b2 -->|削除を要求| c1
    c1 -->|削除する| e1
    e1 -->|完了| c1
    c1 -->|削除結果を表示する| b1
    c1 -.->|"［キャンセルの場合］管理画面へ戻す"| b1
```

## チェックリスト

- [x] バウンダリ・コントローラ・エンティティの3種類が正しく区別されている
- [x] バウンダリとエンティティが直接つながっていない
- [x] アクターがバウンダリを介さずにコントローラ／エンティティへ直接つながっていない
- [x] ユースケース記述の基本フロー・代替フローの文が、すべて図の中に反映されている
- [x] 基本フローは実線、代替フローは破線で描き分けられている
- [x] 代替フローの矢印に［条件］のラベルが添えられている
- [x] バウンダリが「●●画面」、コントローラが「●●を●●する」、エンティティが「●●」の日本語表記で統一されている
- [x] 英単語（実装名）がロバストネス図に紛れ込んでいない
- [x] 1つのコントローラに処理が詰め込まれすぎていない
